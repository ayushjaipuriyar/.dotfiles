{
  // "layer": "top", // Waybar at top layer
  // "position": "bottom", // Waybar position (top|bottom|left|right)		
  // "height": 30, // Waybar height (to be removed for auto height)
  // "width": 2560, // Waybar width
  // Choose the order of the modules
  "modules-left": ["sway/workspaces"],
  "modules-right": [ "custom/weather", "memory", "cpu", "temperature", "network", "battery","pulseaudio",  "backlight", "tray", "clock"],

  // Modules configuration
  "sway/workspaces": {
    "disable-scroll": false,
    "all-outputs": true,
    "format": "{icon}",
    },
  "tray": {
	"spacing":10,
  },
  "clock": {
    "timezone": "Asia/Kolkata",
    "format": "{:%A, %B %d %I:%M %p}",
    //"format-alt": "{:%A, %B %d}",
    "tooltip": false,
    "on-click" : "bluetoothctl connect 74:45:CE:24:42:52"
  },
  "cpu": {
    "format": "{usage}  ",
    "tooltip": false,
    "interval" : 1
  },
  "memory": {
    "format": "{percentage}%  ",
    "interval": 1 
  },
  "temperature": {
    "thermal-zone": 2,
    "hwmon-path": "/sys/class/hwmon/hwmon1/temp1_input",
    "critical-threshold": 80,
    "format-critical": "{temperatureC}°C {icon}",
    "format": "{temperatureC}  {icon}",
    "interval": 1,
    "format-icons": ["", "", ""]
  },
  "backlight": {
    "interval": 5,
    "format": "{icon}",
    "format-alt": "{percent}% {icon}",
    "format-alt-click": "click-right",
    "format-icons": ["🌑", "🌒", "🌓", "🌔", "🌕",],
    "on-scroll-down": "brightnessctl -c backlight set +5%",
    "on-scroll-up": "brightnessctl -c backlight set 5%-",
    "on-click" : "brightnessctl -c backlight set 10%"
  },
  "battery": {
    "states": {
      // "good": 95,
      "warning": 30,
      "critical": 20
    },
    "format": "{capacity}% {icon}",
    "format-charging": "{capacity}% ",
    "format-plugged": "{capacity}% ",
    "format-alt": "{time} {icon}",
    // "format-good": "", // An empty format will hide the module
    // "format-full": "",
    "format-icons": ["", "", "", "", ""]
  },
  "network": {
    "format-wifi": "直 {essid}",
    "format-ethernet": "{ifname}: {ipaddr}/{cidr} ",
    "format-linked": "{ifname} (No IP) ",
    "format-disconnected": "Disconnected ⚠",
    "format-alt": "{ifname}: {ipaddr}/{cidr}",
    "interval": 10,
  },
  "pulseaudio": {
    "scroll-step": 1, // %, can be a float
    "format": "{icon} {volume}% {format_source}",
    "format-bluetooth": " {icon} {volume}% {format_source}",
    "format-bluetooth-muted": "  {format_source}",
    "format-muted": " {format_source}",
    "format-source": " {volume}%",
    "format-source-muted": "",
    "format-icons": {
      "default": "",
      "speaker" : "蓼",
    },
    "on-click": "pavucontrol",


  },
  "custom/weather": {
    "exec": "curl 'https://wttr.in/Noida?format=1'",
    "interval": 3600
  }
},
}
